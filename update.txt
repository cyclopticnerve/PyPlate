
project
-------

run pybaker and change version
git stage all changes
git commit/push w/ msg "baked x.x.x"
git graph, "create tag...", tag name="vx.x.x", push remote

github
------

create release using tag
add dist zip

pyplate
-------

uninstall old cnlib
edit reqs for new cnlib:
    CNLib @ git+https://github.com/cyclopticnerve/CNLib@vx.x.x
pip install -r requirements.txt
update template reqs:
    CNLib @ git+https://github.com/cyclopticnerve/CNLib@vx.x.x
