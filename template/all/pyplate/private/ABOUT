This file is here to make sure git includes this directory in the repo because 
git does not currently allow empty directories to be synced.

This directory will be filled with the pyplate private files, as created by
pybaker.
