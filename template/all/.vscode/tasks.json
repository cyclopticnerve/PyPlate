{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PyBaker",
            "type": "shell",
            "command": "${userHome}/__PP_DEV_PP__/src/pybaker.py",
            "options": {
                "cwd": "${workspaceFolder}",
                "args": ["-t"]
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}